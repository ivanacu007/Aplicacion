<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aplicacion">

    <application
        android:allowBackup="true"
        android:configChanges="orientation"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FondasMenu"
            android:label="@string/title_activity_fondas_menu"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".CarriageMenu"
            android:label="@string/title_activity_carriage_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ServicesMenu"
            android:label="@string/title_activity_services_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FoodMenu"
            android:label="@string/title_activity_food_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>